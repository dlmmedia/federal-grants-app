import type { Metadata } from "next";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "@/components/ui/sonner";

export const metadata: Metadata = {
  title: "GrantScope - Federal Grant Search & Application Platform",
  description: "Discover, analyze, and apply to U.S. federal grant opportunities with AI-powered assistance. Search thousands of grants from Grants.gov.",
  keywords: ["grants", "federal grants", "grant search", "grant application", "Grants.gov", "funding"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="antialiased">
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
